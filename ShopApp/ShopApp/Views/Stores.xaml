<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewModels="clr-namespace:ShopApp.ViewModels"
    x:Class="ShopApp.Views.Stores">
    <ContentPage.BindingContext>
        <viewModels:StoresViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <Grid RowDefinitions="auto, auto, auto" Margin="10, 10, 10, 5">
            <Entry Text="{Binding SearchWord}" Placeholder="Pesquisar" PlaceholderColor="Black" TextColor="Black" BackgroundColor="White" />
            <Button Command="{Binding SearchCommand}" Text="OK" Grid.Row="1" />
            <CollectionView ItemsSource="{Binding Establishments}" Grid.Row="2">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Label Text="{Binding Name}" TextColor="Black"/>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>
    </ContentPage.Content>
</ContentPage>
